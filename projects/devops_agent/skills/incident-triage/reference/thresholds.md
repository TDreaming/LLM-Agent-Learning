# 分诊参考阈值（示例）

以下为示例性的判定参考，实际可按业务调整：

| 指标 | 关注阈值 | 说明 |
|---|---|---|
| CPU 使用率 | > 85% | 持续偏高需关注扩容/热点 |
| 内存使用率 | > 85% | 接近上限可能触发 OOM |
| p99 延迟 | > 基线 + 3σ | 显著劣化视为异常 |
| 错误率 | > 1% | 超过即需排查 |
| 副本就绪 | ready < desired | 存在未就绪实例 |
